{
  "name": "@teacher-assistant/diff-orchestrator",
  "version": "0.0.0",
  "private": true,
  "description": "M11 diff-orchestrator: receives DE-IDENTIFIED differentiation prompts, calls the external LLM, returns a scaffold. Logs nothing of prompt/response bodies; rejects any payload carrying initials (defense in depth). Scaffold only (health endpoint).",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"(no tests yet — diff-orchestrator is a scaffold)\" && exit 0"
  },
  "dependencies": {
    "@teacher-assistant/schema": "workspace:*",
    "fastify": "~5.12.0"
  },
  "devDependencies": {
    "@types/node": "~26.2.0",
    "tsx": "~4.23.12",
    "typescript": "~5.9.3"
  }
}

{
  "name": "@teacher-assistant/schema",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript entity types + the encrypted-vs-cleartext classification + the opaque-id contract. CO-OWNED with the data-model-integration agent (entity fields, IC/Toddle export formats) — implements architecture/data-model.md.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vitest": "~4.1.10"
  }
}

{
  "name": "@teacher-assistant/schema",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types + validators. CO-OWNED with the data-model-integration agent (entity fields, IC/Toddle export formats). Scaffold only — concrete entities land with the data-model spec.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"(no tests yet — @teacher-assistant/schema is a scaffold)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}

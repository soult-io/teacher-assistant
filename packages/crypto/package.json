{
  "name": "@teacher-assistant/crypto",
  "version": "0.0.0",
  "private": true,
  "description": "M0 crypto/keyring core: key hierarchy (master key + per-period DEKs + device keypairs), XChaCha20-Poly1305 record AEAD, X25519 key wrapping, Argon2id paper-recovery wrap, OOB-QR device + para enrollment, DEK rotation/revocation. Client-side only — the server never receives an unwrapped key.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@teacher-assistant/schema": "workspace:*",
    "libsodium-wrappers-sumo": "0.8.4"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "~5.0.1",
    "typescript": "~7.0.2",
    "vitest": "~5.0.1"
  }
}
